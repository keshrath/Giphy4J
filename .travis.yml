language: java

sudo: false

notifications:
  email: false

cache:
  directories:
  - $HOME/.m2

jdk:
  - oraclejdk8

os:
  - linux

install: true

script:
  - mvn test -B
  - mvn cobertura:cobertura

after_script:
  - bash <(curl -s https://codecov.io/bash)
